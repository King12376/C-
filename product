#include <iostream>

using namespace std;

int main()
{
    int x, product = 1;
    
    do
    {
        cout << "Enter a number: ";
        cin >> x;
        if(x > 0)
            product *= x;
    }
    while (x != 0);
    
    cout << product;
    
    return 0;
}
